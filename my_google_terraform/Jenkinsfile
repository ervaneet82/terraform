pipeline {
  agent any
  environment {
    SVC_ACCOUNT_KEY = credentials('google-terraform')
  }
  stages {
    stage('credentials') {
      steps {
        echo '$SVC_ACCOUNT_KEY'
      }
    }
  }
}
